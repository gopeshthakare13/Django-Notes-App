apiVersion: v1
kind: Service

metadata:
  name: django-service
  namespace: django

spec:
  selector:
    app: django

  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
                
  type: NodePort
